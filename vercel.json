{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "nodeVersion": "18.x",
        "installCommand": "npm install --include=dev",
        "buildCommand": "npm run build"
      }
    }
  ],
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  }
}